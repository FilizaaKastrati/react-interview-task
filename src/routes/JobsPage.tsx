import TaskDashboard from '../components/jobs/TaskDashboard';

export default function JobsPage() {
    return (
        <TaskDashboard 
            title="Job Sites" 
            infoText="View and manage all job sites and their current status." 
        />
    );
}
